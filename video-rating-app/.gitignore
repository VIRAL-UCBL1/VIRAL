# Ignore system-specific files that are generated by the operating system
.DS_Store          # macOS system file
Thumbs.db           # Windows system file

# Ignore virtual environment directories
venv/               # Python virtual environment folder (could be named .venv too)
backend/venv/       # Virtual environment in the backend folder (if exists)

# Ignore IDE and text editor configuration files
.idea/              # JetBrains IDE files (e.g., PhpStorm, IntelliJ)
.vscode/            # Visual Studio Code settings folder

# Ignore frontend build dependencies
lock.json et node_modules)
package-lock.json
node_modules/

# Ignore backend rate-related CSV files (user-specific ratings)
backend/rate/*.csv  # Ignore all CSV files inside backend/rate/

# Ignore logs, temporary files, and swap files
*.log               # Log files generated during runtime
*.tmp               # Temporary files generated by the system or apps
*.swp               # Swap files generated by text editors like Vim

# Ignore video directory (if you don't want to version the video files)
videos/             # Folder where video files are stored

# Ignore Python bytecode and cached files
*.pyc               # Compiled Python bytecode files
__pycache__/        # Python's cache folder that stores bytecode files

# Ignore environment files related to Conda (optional)
# If you don't want to version your environment configuration
backend/venv/       # Conda or other Python environment folder (if used)
